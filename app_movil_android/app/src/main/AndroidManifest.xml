<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Permisos de Bluetooth BLE requeridos -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE"/>

    <!-- Permisos para eclipse paho (utiliza wake_lock) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Permisos de Ubicación  -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App_movil_android"
        tools:targetApi="31">

        <!-- Servicio Eclipse Paho para MQTT -->
        <service
            android:name="org.eclipse.paho.android.service.MqttService"
            android:exported="false"/>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Controllers.DevicesActivity"
            android:exported="false" />

        <activity
            android:name=".Controllers.VerificationActivity"
            android:exported="false" />

        <activity
            android:name=".Controllers.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".Controllers.MonitoreoActivity"
            android:exported="false" />

        <activity
            android:name=".Controllers.HistorialActivity"
            android:exported="false" />

        <activity
            android:name=".Controllers.DetallesRutaActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>

</manifest>