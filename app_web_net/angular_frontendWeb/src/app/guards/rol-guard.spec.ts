import { TestBed } from '@angular/core/testing';

import { rolGuard } from './rol-guard';

describe('rolGuard', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

  it('should be created', () => {
    expect(rolGuard).toBeTruthy();
  });
});